public with sharing class OppCalculatorController_Combined {
    @auraEnabled
    public static void calculateOpp(String userType){
        OppCalculatorController_Combined.calculateOpp(userType);
    }
}